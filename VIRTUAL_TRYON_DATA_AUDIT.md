# Virtual Try-On 데이터/코디 구성 점검 리포트

## 1) 현재 어떤 방식으로 코디가 만들어지는가
- `/api/generate` 호출 시 백그라운드에서 기본 8장의 이미지를 생성합니다.
- 이미지별 프롬프트는 `build_prompt(...)` 결과에 `variation` 문구(예: coat+trousers, blazer+slacks...)를 붙여 다양화를 만듭니다.
- 즉, **실제 의류 상품 DB를 조합해서 입히는 방식이 아니라 텍스트 프롬프트 기반 합성**입니다.

## 2) 추천 상품은 어떻게 붙는가
- 결과 조회(`/api/results/{task_id}`)에서 생성된 이미지들의 `category` 목록을 뽑아 `clothing_items`를 카테고리(+성별)로 조회합니다.
- 매칭 실패 시 성별 기준 최신 아이템 fallback을 반환합니다.
- 프론트에서는 생성 이미지 배열과 추천 상품 배열을 **인덱스 modulo(`i % catalog.length`)**로 임시 매칭합니다.
- 따라서 현재는 "이 이미지에 실제로 이 상품이 입혀졌다"는 정밀 매핑이 아닙니다.

## 3) 현재 프로젝트에 들어있는 데이터(샘플/정적 이미지)
- 샘플 카탈로그 CSV는 6개 아이템(카테고리: active/daily/date/office, 성별: men/women)입니다.
- CSV의 `image_path` 6개 파일은 `backend/static/images`에 모두 존재합니다.
- `backend/static/images` 폴더에는 총 14개 PNG(모두 1024x1024)가 들어있습니다.
  - 즉, 샘플 CSV에 연결된 6개 외에 추가 이미지 8개가 존재합니다.

## 4) "임의로 생성된/대체" 데이터 여부
- 프론트에서 결과 렌더링 시 추천 상품이 비어 있으면 이름을 `스타일 N`, 가격/재고/매장/설명을 "정보 없음"으로 채웁니다.
- 진행률도 실제 결과가 없을 때 95%까지 서서히 증가시키는 시뮬레이션 로직이 있습니다.
- 결론적으로 **UI 레벨의 fallback/placeholder 데이터는 존재**합니다.

## 5) 이미지 품질 관련 결론
- 저장된 정적 PNG들은 파일 크기/해시 기준으로 서로 다른 이미지(중복 아님)입니다.
- 다만, 이 리포트는 "상품-착장 정확 매핑" 관점에서 보면 현재 구조가 프롬프트 기반이므로
  "정확한 상품 착장 증명"이 어렵고, 결과적으로 체감상 부정확/랜덤하게 보일 수 있다는 점이 핵심입니다.

## 6) 개선 제안(짧게)
1. 생성 이미지에 사용된 상품 ID 목록을 서버에서 함께 저장/반환(예: `used_item_ids`).
2. 프론트의 modulo 매칭 제거 후, 서버가 내려준 명시적 매핑만 렌더링.
3. 샘플 데이터 확장(카테고리·성별·핏별 충분한 수량) 및 검수된 실제 상품 컷으로 교체.
