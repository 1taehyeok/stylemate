# StyleMate 실데이터/DB 전환: 역할 분담 & 권장 진행 방법

DB 경험이 많지 않아도 안전하게 진행할 수 있도록, **"제가 해줄 일" / "당신이 할 일"**을 분리해 가장 현실적인 협업 방식을 정리했습니다.

## 결론(가장 권장)

**권장 방식은 PR 기반 반복 개발**입니다.

1. 제가 기능 단위로 코드 작성 + 테스트 + PR 설명까지 준비
2. 당신은 PR pull 받아 로컬에서 실행/검증
3. 이상 있으면 피드백 → 제가 수정 PR
4. 검증되면 main에 병합

즉, 질문하신 것처럼 **"제가 PR 만들어주고, 당신이 pull해서 돌려보는 방식"이 맞고 가장 권장**됩니다.

---

## 1) 제가 해줄 수 있는 것 (Agent)

아래는 제가 대부분 직접 처리할 수 있습니다.

- DB 스키마 설계안 작성 (테이블/관계)
- SQLAlchemy 모델/라우터/스키마 코드 구현
- Alembic 마이그레이션 파일 작성
- CSV import/seed 스크립트 작성
- 프론트의 랜덤/목업 데이터 제거 및 실데이터 바인딩
- 테스트 코드 추가(백엔드 API/프론트 매핑)
- 실행 방법 문서화 및 체크리스트 제공

---

## 2) 당신이 해야 하는 것 (Owner)

아래는 보통 저장소 소유자/실사용자가 해야 합니다.

- 실제 상품 데이터 준비(엑셀/CSV)
  - 예: 이름, 가격, 카테고리, 성별, 재고, 이미지 경로
- 환경변수 설정
  - API Key, DB_URL, 실행 환경 값
- 로컬/서버에서 실행 검증
  - 앱 UI/흐름/데이터 정합성 확인
- 최종 병합 결정
  - 코드/기능이 요구사항에 맞는지 판단 후 merge

---

## 3) 실제 진행 순서 (초보자 친화)

### Step A. 제가 먼저 할 일
- 1차 PR: **기반 작업**
  - Alembic 도입
  - DB 모델 정리
  - 최소 seed/import 스크립트 추가

### Step B. 당신이 할 일
- PR checkout 후 아래 실행
  - 백엔드 실행
  - 마이그레이션 적용
  - 샘플 데이터 import
  - 프론트 실행 후 화면 확인

### Step C. 제가 다음으로 할 일
- 2차 PR: **실데이터 연결**
  - 결과 API에 추천 상품 포함
  - 프론트 랜덤 값 제거
  - 상세 페이지 실데이터화

### Step D. 당신이 확인할 일
- “랜덤값이 완전히 사라졌는지”
- “상품/재고/가격이 실제 데이터와 일치하는지”

---

## 4) 자주 하는 질문

### Q1. "PR pull해서 돌려보면 되는 거야?"
네, 맞습니다. 가장 안전하고 권장되는 방식입니다.

### Q2. "한 번에 다 바꾸는 게 좋아?"
아니요. **작은 PR로 나눠서** 가는 게 훨씬 안전합니다.
- 이유: 문제 발생 시 원인 파악/롤백이 쉬움

### Q3. "DB를 잘 모르는데 괜찮아?"
괜찮습니다. 처음에는
- SQLite + seed 데이터
- 마이그레이션 적용/되돌리기
이 2가지만 익혀도 충분히 진행 가능합니다.

---

## 5) 권장 브랜치/PR 운영 규칙

- 브랜치: `feature/db-migration-01`, `feature/real-data-binding-02` 처럼 기능 단위
- PR 크기: 1 PR = 1 목표(예: Alembic 도입만)
- 각 PR에 포함할 것
  - 변경 요약
  - 실행 명령어
  - 확인 포인트(체크리스트)
  - 롤백 방법

---

## 6) 지금 바로 시작하려면

가장 좋은 첫 작업은 아래입니다.

1. **PR-1**: Alembic + 초기 마이그레이션 + 샘플 카탈로그 import
2. **PR-2**: 결과 API에서 추천 상품 반환
3. **PR-3**: 프론트 랜덤 데이터 제거 및 실데이터 렌더링

원하면 제가 바로 **PR-1부터 실제 코드로 순서대로 만들어서** 올려드릴 수 있습니다.
